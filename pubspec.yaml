name: manager
description: "yet another dependency injection and architecture enforcement package"
publish_to: "none"
version: 3.13.0

environment:
  sdk: ^3.12.0
dependencies:
  flutter:
    sdk: flutter
flutter:
  uses-material-design: true
